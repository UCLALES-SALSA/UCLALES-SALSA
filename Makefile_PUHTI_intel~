###############################################################
#
# Location of code (in $ROOT) and location where model is to be built $BIN
#
ROOT      :=$(shell dirname $(realpath $(lastword $(MAKEFILE_LIST))))
BIN       = $(ROOT)/bin
ARCH      := $(shell uname)
#
# Generic Variables
#
SRC         =$(ROOT)/src
SRC_UTIL    =$(SRC)/src_util
SRC_LES     =$(SRC)/src_LES
SRC_SALSA   =$(SRC)/src_salsa
SRC_RAD     =$(SRC)/src_rad
SRC_EMIS    =$(SRC)/src_emission
SRC_SHARED  =$(SRC)/src_shared
SRC_IO      =$(SRC)/src_IO
SRC_VARS    =$(SRC)/src_vars

VPATH = $(SRC_LES):$(SRC_SALSA):$(SRC_UTIL):$(SRC_RAD):   \
        $(SRC_EMIS):$(SRC_SHARED):$(SRC_IO):$(SRC_VARS):  \
	$(SRC)

ECHO    = /bin/echo
RM      = /bin/rm -f

MPI = $(OPENMPI_INSTALL_ROOT)
MPILIB = -L$(MPI)/lib -lmpi
MPIINC = -I$(MPI)/include

NCDF = /appl/spack/v018/install-tree/oneapi-2022.1.0/netcdf-fortran-4.5.4-7urzny
NCDFLIB = -L$(NCDF)/lib -lnetcdff
NCDFINC = -I$(NCDF)/include

LIBS = '$(NCDFLIB) $(MPILIB)'
F90 = ifort
MPIF90 = mpifort
FFLAGS = -O2 -march=native -real-size 64 -fp-model precise #-stand f08 
SEQFFLAGS = -I$(SRC) $(NCDFINC) $(MPIINC)
MPIFFLAGS = -I$(SRC) $(NCDFINC) $(MPIINC)


LES_OUT_MPI=$(BIN)/les.mpi

LES_OUT_SEQ=$(BIN)/les.seq

default: mpi

all:  mpi seq

seq: $(LES_OUT_SEQ)

mpi: $(LES_OUT_MPI)

$(LES_OUT_SEQ): 
	cd $(SRC); $(MAKE) LES_ARC=seq                      \
	FFLAGS='$(FFLAGS) $(SEQFFLAGS)' F90=$(F90)          \
	OUT=$(LES_OUT_SEQ) LIBS=$(LIBS) SRCUTIL=$(SRC_UTIL) \
	SRCLES=$(SRC_LES) SRCSALSA=$(SRC_SALSA)             \
	SRCRAD=$(SRC_RAD) SRCEMIS=$(SRC_EMIS)               \
	SRCSHARED=$(SRC_SHARED) SRCIO=$(SRC_IO)             \
	SRCVARS=$(SRC_VARS)

$(LES_OUT_MPI):
	cd $(SRC); $(MAKE) LES_ARC=mpi                      \
	FFLAGS='$(FFLAGS) $(MPIFFLAGS)' F90=$(MPIF90)       \
	OUT=$(LES_OUT_MPI) LIBS=$(LIBS) SRCUTIL=$(SRC_UTIL) \
	SRCLES=$(SRC_LES) SRCSALSA=$(SRC_SALSA)             \
	SRCRAD=$(SRC_RAD) SRCEMIS=$(SRC_EMIS)               \
	SRCSHARED=$(SRC_SHARED) SRCIO=$(SRC_IO)             \
	SRCVARS=$(SRC_VARS)

.PHONY: $(LES_OUT_SEQ) 
.PHONY: $(LES_OUT_MPI)

#
# cleaning
# --------------------
#
clean: cleanmpi cleanseq 
	$(RM) $(SRC)/*mod $(SRC)/*.o

cleanmpi:
	$(ECHO) "cleaning mpi model"
	$(RM) core $(LES_OUT_MPI) $(SRC)/mpi/*mod $(LES_ARC_MPI)

cleanseq:
	$(ECHO) "clean sequential model"
	$(RM) core $(LES_OUT_SEQ) $(SRC)/seq/*mod $(LES_ARC_SEQ)

FORCE: 
.PRECIOUS: $(LIBS)
